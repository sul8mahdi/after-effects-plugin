# القرار التقني — Icon Motion Pro

> Technical Decision Record. آخر تحديث: 2026-07-24.

## 1. المشكلة

نحتاج لوحة (Panel) احترافية داخل After Effects 2026: واجهة ثنائية اللغة
(عربي/إنجليزي، RTL)، ومحرك تحليل/تحريك يصل بعمق إلى Shape Layers وKeyframes.
الخياران المطروحان لبناء اللوحة: **UXP** أو **CEP**.

## 2. ما تم التحقق منه (يوليو 2026)

- After Effects 2026 لا يزال يعتمد **CEP + ExtendScript** كمسار أساسي للوحات
  والأتمتة العميقة. دعم UXP في AE لم يبلغ تكافؤ ExtendScript في الوصول إلى
  Shape Layers / Keyframes / matchNames (على عكس Photoshop و InDesign و Premiere).
- الوصول العميق الذي يحتاجه هذا المشروع تحديداً — `Trim Paths`,
  `KeyframeEase`, `setTemporalEaseAtKey`, matchNames — ناضج **فقط** في ExtendScript.

المصادر: Adobe After Effects Scripting Guide (matchNames)، Adobe Community
(UXP for After Effects)، تحليلات مطوّري الطرف الثالث لحالة ExtendScript 2026.

## 3. القرار

**معمارية من طبقتين:**

```
┌─────────────────────────────────────────────┐
│  client/  — لوحة CEP (Chromium + Node)        │
│  HTML / CSS / JavaScript حديث (ES6+)          │
│  الترجمة، RTL، الحالة، المعاينة، الأزرار       │
└───────────────┬─────────────────────────────┘
                │  CSInterface.evalScript(...)
┌───────────────▼─────────────────────────────┐
│  host/  — محرك ExtendScript (ES3 فقط)         │
│  تحليل الأيقونة، الحركة، Undo، Logging          │
│  يلتزم حرفياً بقواعد CLAUDE.md                  │
└───────────────┬─────────────────────────────┘
                │  After Effects DOM API
┌───────────────▼─────────────────────────────┐
│  After Effects 2026 — عناصر أصلية فقط          │
│  Shape Layers · Keyframes · Trim · Expressions │
└─────────────────────────────────────────────┘
```

## 4. لماذا

1. **الوصول العميق متاح فقط في ExtendScript** — لا بديل ناضج في AE 2026.
2. **الواجهة تتحرر من ES3** — CEP يشغّل Chromium، فالواجهة JS حديث كامل،
   وهذا يجعل RTL وثنائية اللغة والمعاينة الحية عملية بلا ألم ES3.
3. **توافق تام مع `CLAUDE.md`** — طبقة `host/` تبقى ES3 نقية وتخضع لكل قواعد
   الملف (§1 المحظورات، §3 matchNames، §4 تسلسل الـ easing).
4. **الاستقلالية (§16 من البرومت)** — المخرجات عناصر AE أصلية تعمل على جهاز
   بلا الإضافة.
5. **macOS أولاً ثم Windows** — CEP يعمل بشكل متطابق على النظامين.

## 5. الأثمان المقبولة

- CEP مسار قديم أعلنت Adobe نية إيقافه مستقبلاً، لكنه مدعوم في AE 2026 وهو
  المسار الوحيد العملي اليوم. عزلنا الواجهة عن المحرك حتى لو هاجرنا للـ UXP
  لاحقاً، يبقى محرك `host/` كما هو ونعيد بناء `client/` فقط.
- CSInterface.js مكتبة Adobe يجب تضمينها (vendored). في هذه المرحلة كتبنا
  غلافاً مصغّراً فوق `window.__adobe_cep__` يكفي لمسار `evalScript`؛ يمكن
  استبداله بالنسخة الرسمية الكاملة دون تغيير بقية الكود.

## 6. انحراف مقصود عن `CLAUDE.md` §7

`CLAUDE.md` §7 وصف بنية مجلدات لمرحلة "MVP — المحرك فقط" ونصّ على عدم بناء لوحة
CEP "حتى يُطلب صراحة". المستخدم طلب الآن صراحةً المشروع الكامل، لذا وسّعنا البنية
إلى تخطيط CEP القياسي (`client/` + `host/`). روح `CLAUDE.md` محفوظة: طبقة المحرك
تبقى ES3 وتتبع كل القواعد. هذا الانحراف موثّق هنا عمداً، وليس تجاوزاً صامتاً.
